<h1>Listing taters</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @taters.each do |tater| %>
  <tr>
    <td><%= link_to 'Show', tater %></td>
    <td><%= link_to 'Edit', edit_tater_path(tater) %></td>
    <td><%= link_to 'Destroy', tater, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tater', new_tater_path %>
