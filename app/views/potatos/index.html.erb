<h1>Listing potatos</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @potatos.each do |potato| %>
  <tr>
    <td><%= link_to 'Show', potato %></td>
    <td><%= link_to 'Edit', edit_potato_path(potato) %></td>
    <td><%= link_to 'Destroy', potato, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Potato', new_potato_path %>
